/*
  DÍA 22: localStorage, sessionStorage, JSON

  Instrucciones:
  1. EXTENDER EL TO-DO LIST (día 13) con persistencia:
     - Al agregar tarea, guardar en localStorage
     - Al cargar la página, recuperar tareas guardadas
     - Al eliminar/marcar tarea, actualizar localStorage

  2. Historial de búsquedas:
     - Guardar las últimas 5 búsquedas en localStorage
     - Mostrarlas como sugerencias

  3. Preferencias del usuario:
     - Guardar { tema, idioma, fontSize } en localStorage
     - Aplicar al cargar la página

  +++ EJERCICIO EXTRA +++
  - Crea un wrapper storage que maneje expiración:
    setWithExpiry(key, value, ttlMs)
    getWithExpiry(key)
*/
