/*
  DÍA 11: Sets y Maps

  Instrucciones:
  1. ELIMINAR DUPLICADOS:
     - Función sinDuplicados(array) que devuelva array sin repetidos
     - Usa Set

  2. CONTAR FRECUENCIA DE PALABRAS:
     - Función frecuenciaPalabras(texto) que devuelva Map<string, number>
     - Mostrar las 3 palabras más frecuentes

  3. UNIÓN, INTERSECCIÓN, DIFERENCIA de conjuntos:
     - Usando Sets, implementa:
       unir(a, b), intersectar(a, b), diferir(a, b)

  4. INVENTARIO con Map:
     - Map<string, { precio: number, stock: number }>
     - Funciones: agregarProducto, actualizarStock, eliminarProducto
     - Consultar stock de un producto

  +++ EJERCICIO EXTRA +++
  - Usa WeakSet para evitar que un objeto sea procesado dos veces
  - Crea un caché simple con WeakMap para resultados de funciones
*/
