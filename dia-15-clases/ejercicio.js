/*
  DÍA 15: Clases y POO básica

  Instrucciones:
  1. Clase Animal:
     - constructor(nombre, edad, sonido)
     - método hacerSonido() que devuelva el sonido

  2. Clase Perro que herede de Animal:
     - Propiedad adicional: raza
     - Método moverse() → "corriendo"

  3. Clase Gato que herede de Animal:
     - Propiedad adicional: vidasRestantes (default 7)
     - Método perderVida() → reduce vidas

  4. Clase CuentaBancaria:
     - #saldo privado (private field)
     - depositar(monto), retirar(monto), consultarSaldo()
     - No permitir retiros que dejen saldo negativo

  +++ EJERCICIO EXTRA +++
  - Crea una clase Vector con x, y y métodos:
    sumar(v), restar(v), multiplicar(escalar), magnitud()
*/
