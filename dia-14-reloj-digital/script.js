/*
  DÍA 14: Mini Proyecto 1 — RELOJ DIGITAL

  Requisitos:
  1. Mostrar hora actual actualizada cada segundo (setInterval)
  2. Mostrar también la fecha actual
  3. Botón para alternar entre formato 12h y 24h
  4. Formatear con 2 dígitos (ej: 01 en vez de 1)

  Pistas:
  - new Date() para obtener hora actual
  - getHours(), getMinutes(), getSeconds()
  - Formato 12h: (h % 12) || 12, y agregar AM/PM
  - setInterval(actualizarReloj, 1000)
  - padStart(2, '0') para 2 dígitos
*/

// --- Tu código aquí ---
